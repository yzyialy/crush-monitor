# 隐私说明与免责声明

这份文件说明 **crush-monitor** 会把你的数据放在哪里、发给谁、谁能看到，以及它**不承诺什么**。

最后更新：随代码一起维护；改动数据流向必须同步改本文件。

---

## 1. 一句话总结

**这个版本是「本机版」：没有账号、没有登录、没有服务器端数据库。**
你粘贴的聊天、分析结果、长期观察档案全部存在**你这台电脑的浏览器本地存储（localStorage）**里；
只有分析的时候，会把相关聊天文本发送给**你自己配置的 AI 服务商**（TypeSafe 的 Jev，以及可选的 DeepSeek）。

没有遥测，没有统计上报，没有云端账号，作者这边收不到你的任何数据。

---

## 2. 数据流向

| 数据类型 | 存在哪 | 谁能看到 | 会发给第三方吗 |
|---|---|---|---|
| 粘贴的聊天正文、时间戳 | **浏览器 localStorage**（本机） | 能碰到这台电脑/这个浏览器的人 | **分析时把相关片段发给 TypeSafe；用「深度解读」时还会发给 DeepSeek** |
| 逐句情绪/意图标签、回复评级 | 同上 | 同上 | 否（属于分析结果） |
| 长期观察档案（行为基线、记忆、模式、习惯、反馈） | 同上 | 同上 | 「深度解读」时会把**检索出来的相关片段**发给 DeepSeek |
| API Key | **只在你本机 Node 进程的 `.env` 文件里** | 能读到这台电脑文件的人 | 请求时作为凭证发给对应服务商，**永远不会下发到浏览器** |
| 前端界面草稿、开关 | 同上（localStorage） | 同上 | 否 |
| 诊断日志（可选的错误回报） | 你本机的 `logs/server.log` | 同上 | 否。只记错误文本与浏览器 UA，**不含聊天内容** |

补充事实：

- **不读取微信数据库、不后台监听、不自动收发消息。** 只有你手动复制粘贴进来的文字才会进系统。
- **没有遥测。** 代码里没有埋点、没有崩溃上报、没有使用统计。
- **日志不含正文。** 本机日志只记计数、状态码、耗时、token 数、违规**类别码**；错误回报只记错误文本。
- **前端 bundle 不含密钥。** API Key 只在你本机的 `.env` 里，永不下发到浏览器；仓库有自动化测试守着这条。
- **换浏览器/清站点数据就没了**：localStorage 绑定在具体浏览器上，无痕窗口、换浏览器、清理浏览数据都会导致记录消失。重要数据请自己留意（也可从 DevTools 里导出 localStorage）。

### 发给第三方 AI 服务商的是什么

| 场景 | 发给谁 | 发什么 |
|---|---|---|
| 逐句分析（必用） | TypeSafe（`jev-1.13.0`） | 你粘贴的**这段聊天文本** |
| 深度解读（可选，默认关闭） | DeepSeek | 这段聊天文本 + 程序算好的统计数字 + 从长期观察档案里**检索出来的相关片段** |

这两条是工具工作的前提，无法关闭第一层。**如果不能接受聊天内容离开这台机器，就不要使用它。**

---

## 3. 这个工具**不会**做的事

- 不做人格诊断、不输出依恋类型标签、不给「对方喜欢你的概率」；
- 不教你话术去操纵、PUA、冷处理任何人；
- 不自动发送、自动回复或做任何对外动作；
- 不把模型的自由推测自动升级成「长期事实」——**只有你亲自确认过的才会**；
- 不删除被你的纠正推翻过的推断（保留下来便于复盘，但不作为事实使用）。

这些是刻意的设计约束，不是能力不足。

---

## 4. 关于别人（第三方）的隐私

- 你粘贴的是**两个人的对话**，聊天对象**没有同意**过被分析。
- 请只处理你自己参与的对话，不要分析你不参与的聊天记录。
- 把分析结果当作「自己的一个视角」，不要拿去给对方看，更不要当成对一个人的定论。
- 如果这段关系已经结束、或对方明确表示不愿被分析，最合适的做法是停下来并删掉数据。

---

## 5. 免责声明

1. **原样提供。** 本项目按 MIT 协议「AS IS」提供，不对可用性、准确性、数据完整性做任何担保。
2. **分析结果仅供参考。** 情绪、意图、好感度、回复评级都由模型与统计规则产生，**可能是错的**。「互动投入下降」不等于「关系变差」，更不等于「对方不喜欢你」。
3. **决策责任在你。** 你根据分析结果做出的任何行动，由你自己负责。
4. **数据丢失风险自负。** 浏览器存储可能被清理，换设备不会自动同步（本机版没有服务端）。请自行判断是否需要备份。
5. **费用自负。** 调用 TypeSafe 与 DeepSeek 的额度由你自己的 Key 承担。
6. **不承担间接损失。** 在适用法律允许的最大范围内，作者与贡献者不对任何直接或间接损失、关系后果、名誉损害承担责任。
7. **无隶属关系。** 本项目与腾讯 / 微信、TypeSafe、DeepSeek 均无隶属或背书关系；相关商标归各自所有者。
8. **合法使用。** 请勿用于跟踪、骚扰、勒索、非法取证或任何违法用途。

---

## 6. 仓库里不会出现什么

- 任何真实 API Key（`.env` 被 `.gitignore` 忽略，`.env.example` 只有空占位）
- 任何真实聊天记录、数据库文件（`data/`、`logs/` 全部忽略）
- 任何一台具体服务器的 IP、域名、账号、路径、云厂商信息
- 任何真实账号名或密码

Fork 之后也请同样不要把上面这些提交上去。
