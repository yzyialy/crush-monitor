# 来源与致谢

本项目基于 **FerryCorleone** 的 [`crush-monitor`](https://github.com/FerryCorleone/crush-monitor) 改进而来，以 MIT 协议发布。

上游是一个「本机运行、用自己的 API Key」的小工具：把微信聊天粘贴进来，用 TypeSafe 的 **Jev** 逐句给出情绪、意图与回复评级。**想法和第一版实现都来自上游作者**——没有那一版，就没有这个仓库。

本仓库在上游基础上增加的（都遵守同一条原则：**模型只做解释，所有数字由程序计算**）：

- **第二层「深度解读」**：把一段交流里值得注意的信号整理成七段结构化解读（发生了什么 / 和她平时相比 / 你可能漏掉的信号 / 可能是什么意思 / 为什么这么判断 / 目前不能确定 / 接下来观察什么）。默认关闭，填了 DeepSeek Key 再打开。
- **Interpretation Boundary 安全层**：拦截伪精确数字、读心式断言（「她其实是……」）和命令式关系建议；被拦下的内容只记录违规**类别码**，不记录原文。
- **长期观察**：按人记录行为基线（回复快慢、长短、主动程度等）、长期行为模式、记忆与表达习惯。`model_inferred` 永远不会自动升级成事实，只有用户确认过的才会；被纠正的推断保留下来可复盘，但不作为事实使用。
- **历史对话**：每段聊天单独记录、按最近更新排序，可以随时点回去看当时的内容与已经分析好的标签；「开始新对话」只新开一段，**不会删除任何历史**；每段可以改名。
- **语音 / 图片 / 表情包**：微信复制出来只有 `[语音]`、`[图片]`、`[动画表情]` 占位符，现在可以在占位符后面补一句描述，描述会被明确标注成「用户手动转述，不是原始文字」；不补描述仍然跳过，且不会撑大「回复长度」这类统计口径。
- **粘贴后不自动分析**：输入框足够大，可以先把上面那些描述补好、错字改掉，确认无误再点「分析聊天」（也可按 `Ctrl/Cmd + Enter`）。
- **不静默失败**：任何「点了没反应」都会在页面顶部用红条写明原因，并写进本机日志（只记错误文本，不记聊天内容）。

---

**感谢 FerryCorleone 把这个工具做出来并开源。** 也感谢 TypeSafe 提供 Jev 这种「返回结构化判断而不是长文本」的模型——这个工具的思路几乎是被它的输出形式逼出来的。

上游的版权声明照原样保留在 `LICENSE` 里；本仓库的改动同样以 MIT 协议发布。
