# 关于这个项目是怎么做出来的（请先读这一份）

## 一句话

**除了上游原作者写的那一版，本仓库的全部改动都是 AI 写的；操作 AI 的那个人不懂编程。**

---

## 说清楚一点

- 上游 [`FerryCorleone/crush-monitor`](https://github.com/FerryCorleone/crush-monitor) 是**人写的**：Jev 逐句分析、情绪/意图/回复评级的想法和第一版实现都来自那里。**功劳是原作者的，问题不要算到他头上。**
- 本仓库在上游之上增加的这些东西——第二层「深度解读」、Interpretation Boundary 安全层、长期观察（行为基线/模式/记忆/确认纠错）、历史对话面板、语音图片表情包描述的标注、以及全部的 bug 修复——**都是 AI 写的**。
- 操作 AI 的人（也就是这个仓库的维护者）**不会写代码**：看不懂大部分实现，也没法逐行 review AI 交出来的东西。他做的事情是：提需求、试用、发现不对劲就把现象和日志丢给 AI，让 AI 去定位和修。
- 结果就是：代码里**可能存在**奇怪的写法、注释和实现不一致的地方、多余的分支、以及我们还没发现的 bug。这不是"免责声明式的客套"，是实情。

## 能保证的与不能保证的

**能保证的**（这是维护者唯一拿得出手的质量证据，都是自动跑的）：

- 仓库里的测试**全部离线**（不调用 Jev、不调用 DeepSeek、不需要任何 API Key），`npm test` 可以直接跑；
- 截至目前：测试全绿、`npm run build` 通过、前端产物里不含任何密钥（有测试盯着）；
- 关键链路用**真实模型调用**验证过（真实 Jev 逐句分析、真实 DeepSeek 深度解读），结果记在提交说明与文档里。

**不能保证的**：正确性、安全性、长期可维护性。AI 写的代码可能带着过时的依赖、不安全的默认值、或者在你的环境里才暴露的问题。**用于重要场景之前请自己审查**（或者让 AI 帮你审查）。

## 遇到问题怎么办：请直接用 AI 修

**最有效的做法不是等维护者回复，而是让 AI 去修**。这个仓库的结构就是为这件事准备的：

1. 把**现象 + 报错信息 + 复现步骤**一起交给 AI（Claude / DeepSeek / ChatGPT / 你手边任何一个能读代码的 AI），告诉它仓库地址；
2. 让它先读 [`docs/AI-HANDOFF.md`](docs/AI-HANDOFF.md)——那份文件就是写给 AI 的交接书：文件职责表、13 条硬规则、常见改动索引、以及"改哪里会踩到哪条测试"；
3. 让它改完跑 `npm test` 与 `npm run build`（都能离线跑，AI 可以自己验证）；
4. 有结果就发 PR / Issue，把过程和结论写清楚。

设计上的取舍、为什么某些红线不能碰，也都写在 `docs/ARCHITECTURE.md` 与 `docs/AI-HANDOFF.md` 里，AI 读完基本就能接手。

维护者也会用 AI 处理问题，但**请不要期待他能手写补丁、逐行 review 或者现场调试**——他能提供的帮助是"把问题交给 AI 并验证结果"，节奏取决于他手上的 AI 能力。

## 如果你也是"不懂代码的驾驶员"

那这个仓库可能正好适合你：它是被一个不懂代码的人用 AI 做出来的，里面的文档（尤其是给 AI 看的那几份）也是按"让 AI 接手"来写的。你可以照同样的办法用它、改它、再拿它做出你自己的东西。

祝好用。出问题就交给 AI。
